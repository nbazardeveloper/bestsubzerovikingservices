// "Why choose us" icon marks. Previously these were inline SVGs adapted from
// free-tier Noun Project artwork, which required a visible attribution credit
// (see SiteFooter). Replaced with lucide-react icons — already used
// throughout the rest of the site (SiteFooter, SiteHeader, service-area,
// etc.) — so no attribution is required and the icon set stays visually
// consistent with the rest of the UI.
export {
  Award as YearsIcon,
  Stethoscope as DiagnosticsIcon,
  Tag as PricingIcon,
  Zap as SameDayIcon,
  CalendarClock as SchedulingIcon,
  ShieldCheck as WarrantyIcon,
} from "lucide-react";
